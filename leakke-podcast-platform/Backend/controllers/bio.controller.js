const { getBioService, updateBioService, } = require('../services/bio.service');
const { generalError } = require('../utils/error');

const getBioController = async () => { }

const updateBioController = async () => { }

module.exports = {
  getBioController,
  updateBioController,
}